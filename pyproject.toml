[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "real_llm: tests that need real llm_client functions (not blocked by autouse mock)",
]
filterwarnings = [
    "ignore::urllib3.exceptions.NotOpenSSLWarning",
]
